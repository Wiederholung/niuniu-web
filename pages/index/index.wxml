
<!-- 轮播 -->
  <swiper indicator-dots  autoplay interval="3555" circular>
    <block wx:for="{{imgUrls}}">
      <swiper-item>
        <image src="{{item}}" class="slide-image" />
      </swiper-item>
    </block>
  </swiper>
<!-- 中部 -->
<view class="category">
    <!-- <navigator class="category_item" wx:for="{{iconArray}}" url="/pages/test1/test1?id={{item.id}}&title={{item.iconText}}"> -->
    <navigator class="category_item" wx:for="{{iconArray}}" url="/pages/selfStudy/selfStudy">
        <image class="category_item_image" src="{{item.iconUrl}}"></image>
        <text class="category_item_text">{{item.iconText}}</text>
    </navigator>
</view>

